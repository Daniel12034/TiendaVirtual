{
  "name": "ecommerce-core-ts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "npm run build && node scripts/start.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
